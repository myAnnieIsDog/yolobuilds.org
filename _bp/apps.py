from django.apps import AppConfig


class BuildingPermitsConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "_bp"
    verbose_name = "Building Permits"
    verbose_name_plural = "Building Permits"
